name: Basic validation

on:
  pull_request:
    paths-ignore:
      - '**.md'
  push:
    branches:
      - main
      - releases/*
    paths-ignore:
      - '**.md'

jobs:
  call-basic-validation:
    name: Basic validation
    uses: priya-kinthali/reusable-workflows/.github/workflows/basic-validation.yml@test-esm
    with:
      node-version: '24.x'
